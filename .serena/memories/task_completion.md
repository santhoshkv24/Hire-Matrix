# Task completion checks
- Frontend changes: run `npm run lint` and `npm run build` in frontend.
- Backend changes: start with `npm run dev` in backend and hit `/api/v1/health` for a quick sanity check.
- Note: backend has no automated tests configured yet.